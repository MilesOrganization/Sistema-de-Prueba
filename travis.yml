language: python
python:
  - "2.7"

# install:
  # - pip install flake8
  # - pip install autopep8
  # - pip install -r requirements.txt

script:
  - python -m compileall ./
  - python -m doctest ./project.py
